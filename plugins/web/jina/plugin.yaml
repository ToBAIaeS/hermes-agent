name: web-jina
version: 1.0.0
description: "Jina Reader — free, self-hosted URL-to-markdown extractor. Requires JINA_READER_URL pointing at your instance. Extract-only; pair with a search provider (searxng, ddgs, brave-free) for web_search."
author: Tobi
kind: backend
provides_web_providers:
  - jina
